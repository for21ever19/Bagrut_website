# 🎨 Веб-дизайн проекта Bagrut Website

**Полная документация по дизайн-системе образовательной платформы для подготовки к экзаменам Багрут**

---

## 📋 Содержание

1. [Концепция дизайна](#концепция-дизайна)
2. [Цветовая палитра](#цветовая-палитра)
3. [Типографика](#типографика)
4. [Компоненты UI](#компоненты-ui)
5. [Анимации и взаимодействия](#анимации-и-взаимодействия)
6. [Макеты страниц](#макеты-страниц)
7. [Паттерны дизайна](#паттерны-дизайна)
8. [Адаптивность](#адаптивность)
9. [Руководство по использованию](#руководство-по-использованию)

---

## 🎯 Концепция дизайна

### Философия

Проект использует концепцию **"Hand-Drawn UI"** (ручная рисовка) — органичный, теплый дизайн, имитирующий физический скетчбук или тетрадь. Это создает ощущение близости, дружелюбности и образовательной среды.

### Ключевые принципы

1. **Органичность** — элементы выглядят нарисованными от руки, с легкой неидеальностью
2. **Теплота** — использование теплых, земляных цветов создает уютную атмосферу
3. **Ясность** — четкая иерархия и структура для легкого восприятия учебного материала
4. **Интерактивность** — плавные анимации и микро-взаимодействия для вовлеченности
5. **Фокус на контенте** — дизайн не отвлекает от учебного материала

### Визуальная метафора

- **Рюкзак** — центральный элемент, символизирующий учебный процесс
- **Папки** — предметы, организованные как физические папки в рюкзаке
- **Тетрадь** — карточки с декоративными линиями, как в школьной тетради
- **Ручная рисовка** — тени и границы выглядят нарисованными от руки

---

## 🎨 Цветовая палитра

### Основные цвета

Цвета определены в `tailwind.config.ts` и используются через Tailwind CSS классы.

#### Фон и базовые цвета

| Цвет | Hex | Tailwind класс | Описание | Использование |
|------|-----|----------------|----------|---------------|
| **Canvas** | `#F2E3CB` | `bg-canvas` | Основной фон | Фон всех страниц, создает теплую, персиково-бежевую атмосферу |
| **Ink** | `#5C6B44` | `text-ink`, `border-ink` | Основной текст и контуры | Текст, границы, иконки. Оливково-коричневый цвет |
| **Ink Shadow** | `#6B6644` | - | Тень для hand-drawn эффекта | Используется в `box-shadow` для создания эффекта ручной рисовки |
| **Accent Gold** | `#E1C858` | `accent-gold`, `bg-accent-gold` | Акценты и интерактивные элементы | Hover-состояния, прогресс-бары, выделения |

#### Цвета предметов

Каждый предмет имеет свой уникальный цвет для визуальной идентификации:

| Предмет | Hex | Tailwind класс | Описание |
|---------|-----|----------------|----------|
| **История** | `#E6B89C` | `folder-history` | Приглушенная терракота |
| **Эзрахут** | `#9FB8AD` | `folder-civics` | Мягкий серо-зеленый |
| **Танах** | `#D4C5A8` | `folder-tanakh` | Бежево-песочный |
| **Литература** | `#C7B2BE` | `folder-lit` | Нежно-розовый |

#### Дополнительные цвета

| Цвет | Hex | Tailwind класс | Использование |
|------|-----|----------------|---------------|
| **Белый** | `#FFFFFF` | `bg-white` | Фон карточек, кнопок |
| **Кремовый** | `#FFFCF5` | `bg-[#FFFCF5]` | Фон карточек контента |
| **Серый светлый** | `#F8F9FA` | `bg-gray-50` | Фон информационных блоков |
| **Серый средний** | `#E5E7EB` | `bg-gray-200` | Границы, разделители |

### Использование цветов

#### Правила применения

1. **Фон страниц**: всегда `bg-canvas` для единообразия
2. **Текст**: `text-ink` для основного текста, `text-ink opacity-70` для вторичного
3. **Границы**: `border-ink` с толщиной `2px` для основных элементов
4. **Акценты**: `accent-gold` для hover-состояний и интерактивных элементов
5. **Карточки**: `bg-[#FFFCF5]` или `bg-white` с `border-2 border-ink`

#### Примеры кода

```tsx
// Основной фон страницы
<div className="min-h-screen bg-canvas">

// Карточка с hand-drawn эффектом
<div className="bg-[#FFFCF5] border-2 border-ink p-8 rounded-lg"
     style={{ boxShadow: '4px 4px 0px rgba(107, 102, 68, 0.2)' }}>

// Текст с правильной иерархией
<h1 className="text-4xl font-serif font-bold text-ink">
<p className="text-ink opacity-70">
```

---

## ✍️ Типографика

### Шрифты

Проект использует два шрифта из Google Fonts для создания контраста между заголовками и текстом.

#### Merriweather (Serif)

- **Использование**: Заголовки (h1, h2, h3, h4, h5, h6)
- **Начертания**: 400 (Regular), 700 (Bold)
- **Характер**: Классический шрифт с засечками, создает академический, образовательный тон
- **Применение**: Все заголовки, названия предметов, важные метки

```css
font-family: 'Merriweather', serif;
```

#### Nunito (Sans-serif)

- **Использование**: Основной текст, параграфы, интерфейсные элементы
- **Начертания**: 400 (Regular), 600 (Semi-bold), 700 (Bold)
- **Характер**: Округлый, дружелюбный гротеск, легко читается
- **Применение**: Тело текста, кнопки, навигация, описания

```css
font-family: 'Nunito', sans-serif;
```

### Иерархия типографики

#### Заголовки

| Уровень | Размер | Вес | Класс | Использование |
|---------|--------|-----|-------|---------------|
| **H1** | `2.25rem` (36px) | Bold | `text-4xl font-serif font-bold` | Главные заголовки страниц |
| **H2** | `1.875rem` (30px) | Bold | `text-3xl font-serif font-bold` | Заголовки разделов |
| **H3** | `1.5rem` (24px) | Bold | `text-2xl font-serif font-bold` | Подзаголовки |
| **H4** | `1.25rem` (20px) | Semi-bold | `text-xl font-serif font-semibold` | Заголовки карточек |
| **H5** | `1.125rem` (18px) | Semi-bold | `text-lg font-serif font-semibold` | Мелкие заголовки |

#### Текст

| Тип | Размер | Вес | Класс | Использование |
|-----|--------|-----|-------|---------------|
| **Основной текст** | `1rem` (16px) | Regular | `text-base` | Параграфы, описания |
| **Большой текст** | `1.125rem` (18px) | Regular | `text-lg` | Важные описания |
| **Малый текст** | `0.875rem` (14px) | Regular | `text-sm` | Вторичная информация |
| **Метки** | `0.9375rem` (15px) | Semi-bold | `text-[15px] font-semibold` | Метки под папками |

### Стили текста

#### Выделения

```tsx
// Жирный текст для акцента
<strong>Важный текст</strong>

// Курсив для выделения
<em>Выделенный текст</em>

// Маркер для терминов
<mark className="bg-yellow-100 px-1">Термин</mark>

// Код для дат и чисел
<code className="bg-gray-100 px-1.5 py-0.5 rounded font-mono font-bold text-red-700">
  1939
</code>
```

#### Цвета текста

- **Основной**: `text-ink` — основной цвет текста
- **Вторичный**: `text-ink opacity-70` — менее важный текст
- **Третичный**: `text-ink opacity-50` — подсказки, мета-информация

### Примеры использования

```tsx
// Заголовок страницы
<h1 className="text-4xl font-serif font-bold text-ink">
  Название предмета
</h1>

// Заголовок раздела
<h2 className="text-2xl font-semibold text-ink mb-4">
  Раздел
</h2>

// Основной текст
<p className="text-ink mb-4">
  Основной текст параграфа
</p>

// Вторичный текст
<p className="text-ink opacity-70 text-lg">
  Описание или подпись
</p>
```

---

## 🧩 Компоненты UI

### 1. Карточки (Cards)

Карточки — основной элемент интерфейса для отображения контента и действий.

#### Базовая карточка

```tsx
<div className="bg-[#FFFCF5] border-2 border-ink p-8 rounded-lg"
     style={{
       boxShadow: '4px 4px 0px rgba(107, 102, 68, 0.2)',
       borderRadius: '8px 10px 9px 8px', // Неидеальный border-radius
     }}>
  {/* Контент */}
</div>
```

**Характеристики:**
- Фон: `#FFFCF5` (кремовый)
- Граница: `2px solid #5C6B44` (ink)
- Тень: `4px 4px 0px rgba(107, 102, 68, 0.2)` (hand-drawn эффект)
- Скругление: слегка неидеальное для органичности
- Отступы: `p-8` (32px)

#### Карточка с декоративными линиями (тетрадь)

```tsx
<div className="bg-[#FFFCF5] border-2 border-ink p-8 rounded-lg relative overflow-hidden">
  {/* Декоративные линии */}
  <div className="absolute top-0 left-0 w-full h-1 bg-ink opacity-10" />
  <div className="absolute top-8 left-0 w-full h-px bg-ink opacity-5" />
  <div className="absolute top-16 left-0 w-full h-px bg-ink opacity-5" />
  <div className="absolute top-24 left-0 w-full h-px bg-ink opacity-5" />
  <div className="absolute top-32 left-0 w-full h-px bg-ink opacity-5" />
  
  {/* Контент с z-index */}
  <div className="relative z-10">
    {/* Контент карточки */}
  </div>
</div>
```

**Использование**: Карточки "Учебные материалы" и "Тренировка" на странице предмета.

### 2. Кнопки (Buttons)

#### Основная кнопка

```tsx
<motion.button
  whileHover={{ scale: 1.1 }}
  whileTap={{ scale: 0.95 }}
  className="p-3 rounded-lg bg-white border-2 border-ink shadow-hand-drawn hover:bg-accent-gold transition-colors"
>
  <Icon className="w-6 h-6 text-ink" />
</motion.button>
```

**Характеристики:**
- Фон: `bg-white`
- Граница: `border-2 border-ink`
- Тень: `shadow-hand-drawn` (4px 4px 0px #6B6644)
- Hover: `hover:bg-accent-gold`
- Анимация: масштабирование при hover/tap

#### Кнопка навигации (назад)

```tsx
<motion.button
  whileHover={{ scale: 1.1 }}
  whileTap={{ scale: 0.95 }}
  onClick={handleBack}
  className="p-3 rounded-lg bg-white border-2 border-ink shadow-hand-drawn hover:bg-accent-gold transition-colors"
  aria-label="Назад"
>
  <ArrowLeft className="w-6 h-6 text-ink" />
</motion.button>
```

### 3. Папки предметов (HandDrawnFolder)

Уникальный компонент, имитирующий физическую папку с PNG-изображением.

```tsx
<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ delay, duration: 0.5 }}
  whileHover={{
    scale: 1.05,
    rotate: [0, -2, 2, -1, 0],
    transition: { type: 'spring', stiffness: 400, damping: 10 }
  }}
  whileTap={{ scale: 0.98 }}
  className="absolute cursor-pointer"
>
  <Image src={imageSrc} alt={label} width={180} height={180} />
  <div className="absolute bottom-[-6px] left-1/2 transform -translate-x-1/2 
                  font-sans text-[15px] font-semibold text-ink"
       style={{ textShadow: '1px 1px 0px #F9E4D4' }}>
    {label}
  </div>
</motion.div>
```

**Характеристики:**
- PNG-изображение с уже нарисованной тенью
- Анимация появления с задержкой
- Hover-эффект: масштабирование и легкое покачивание
- Метка под папкой с тенью текста

### 4. Навигация (Sidebar Navigation)

Боковая панель навигации для учебных материалов.

```tsx
<aside className="w-1/4 min-w-[250px] sticky top-10 self-start">
  <div className="bg-[#FFFCF5] border-2 border-ink rounded-lg p-6 shadow-hand-drawn">
    <h2 className="text-xl font-serif font-bold text-ink mb-4">
      Содержание
    </h2>
    <nav className="max-h-[calc(100vh-120px)] overflow-y-auto scrollbar-hide">
      {/* Ссылки навигации */}
    </nav>
  </div>
</aside>
```

**Стили ссылок:**

```tsx
// Главный раздел (римские цифры)
<a className="font-bold text-gray-800 mt-6 mb-2 text-sm 
               text-accent-gold border-l-2 border-accent-gold pl-3">
  I. ЯДРО ЭКЗАМЕНА
</a>

// Подраздел (арабские цифры)
<a className="text-gray-600 font-medium text-sm py-1 pl-4
               hover:text-accent-gold">
  1. Права человека
</a>
```

### 5. Прогресс-бар

```tsx
<div className="w-full bg-gray-200 rounded-full h-2">
  <motion.div
    className="bg-accent-gold h-2 rounded-full"
    initial={{ width: 0 }}
    animate={{ width: `${progressPercent}%` }}
    transition={{ duration: 0.3 }}
  />
</div>
```

**Характеристики:**
- Фон: `bg-gray-200`
- Заполнение: `bg-accent-gold`
- Высота: `h-2` (8px)
- Анимация: плавное заполнение

### 6. Вопросы и ответы (Training)

#### Карточка вопроса

```tsx
<div className="bg-white rounded-xl shadow-lg max-w-2xl w-full p-8">
  <h2 className="text-xl font-serif font-bold text-ink mb-6">
    {question}
  </h2>
  
  {/* Варианты ответов */}
  <div className="space-y-3">
    {/* Вариант ответа */}
  </div>
</div>
```

#### Вариант ответа

```tsx
// Невыбранный
<button className="w-full text-left p-4 rounded-lg border-2 border-gray-300
                   hover:border-ink hover:bg-gray-50 transition-colors">
  {answer}
</button>

// Выбранный (правильный)
<button className="w-full text-left p-4 rounded-lg border-2 border-green-500
                   bg-green-50">
  {answer}
</button>

// Выбранный (неправильный)
<button className="w-full text-left p-4 rounded-lg border-2 border-red-500
                   bg-red-50">
  {answer}
</button>
```

### 7. Контентные блоки

#### Информационный блок

```tsx
<div className="bg-blue-50 border-l-4 border-blue-500 p-4 rounded-r-lg mb-4">
  <p className="mb-0">
    Информация
  </p>
</div>
```

#### Блок анализа (строфы)

```tsx
<div className="border-l-4 border-indigo-400 pl-4">
  <h3 className="text-lg font-bold text-indigo-900 mb-2">
    Первая строфа
  </h3>
  <p className="mb-0">
    Анализ строфы
  </p>
</div>
```

#### Блок с фоном

```tsx
<div className="bg-gray-50 p-6 rounded-lg border border-gray-200">
  <h3 className="text-xl font-bold text-ink mb-3">
    Заголовок
  </h3>
  <p className="mb-0">
    Контент
  </p>
</div>
```

---

## 🎭 Анимации и взаимодействия

### Библиотека

Проект использует **Framer Motion** для всех анимаций.

### Типы анимаций

#### 1. Появление страницы

```tsx
<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  exit={{ opacity: 0, y: -20 }}
  transition={{ duration: 0.4 }}
>
  {/* Контент */}
</motion.div>
```

#### 2. Появление элементов с задержкой

```tsx
<motion.div
  initial={{ opacity: 0, x: -20 }}
  animate={{ opacity: 1, x: 0 }}
  transition={{ delay: 0.1 }}
>
  {/* Элемент */}
</motion.div>
```

#### 3. Каскадное появление

```tsx
<motion.div
  initial={{ opacity: 0, y: 30 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ delay: 0.2, staggerChildren: 0.1 }}
>
  {/* Дочерние элементы появляются с задержкой */}
</motion.div>
```

#### 4. Hover-эффекты

```tsx
<motion.div
  whileHover={{
    scale: 1.05,
    rotate: 0,
    transition: {
      type: 'spring',
      stiffness: 400,
      damping: 10,
    },
  }}
  whileTap={{ scale: 0.98 }}
>
  {/* Интерактивный элемент */}
</motion.div>
```

#### 5. Покачивание (для папок)

```tsx
<motion.div
  whileHover={{
    scale: 1.05,
    rotate: [0, -2, 2, -1, 0],
    transition: {
      type: 'spring',
      stiffness: 400,
      damping: 10,
    },
  }}
>
  {/* Папка */}
</motion.div>
```

#### 6. Плавное заполнение (прогресс)

```tsx
<motion.div
  initial={{ width: 0 }}
  animate={{ width: `${progressPercent}%` }}
  transition={{ duration: 0.3 }}
  className="bg-accent-gold h-2 rounded-full"
/>
```

### Принципы анимаций

1. **Плавность**: Все анимации используют easing функции
2. **Естественность**: Spring-анимации для интерактивных элементов
3. **Производительность**: Использование transform вместо изменения размеров
4. **Доступность**: Уважение к `prefers-reduced-motion`

---

## 📐 Макеты страниц

### 1. Главная страница (Home)

**Структура:**
```
┌─────────────────────────────────────┐
│         bg-canvas (fullscreen)      │
│                                     │
│         ┌──────────────┐            │
│         │   Рюкзак     │            │
│         │  (центр)     │            │
│         └──────────────┘            │
│                                     │
│  [История]    [Танах]              │
│                                     │
│  [Эзрахут]   [Литература]          │
│                                     │
└─────────────────────────────────────┘
```

**Характеристики:**
- Полноэкранный фон `bg-canvas`
- Центральный контейнер: `900px × 700px`
- Рюкзак: `625px × 625px` в центре
- Папки: абсолютное позиционирование по углам
- Анимация появления с задержками

### 2. Страница предмета (Subject)

**Структура:**
```
┌─────────────────────────────────────┐
│  [←]  Название предмета             │
│                                     │
│  ┌──────────────┐ ┌──────────────┐ │
│  │  Учебные     │ │  Тренировка  │ │
│  │  материалы   │ │              │ │
│  └──────────────┘ └──────────────┘ │
│                                     │
│  ┌──────────────────────────────┐   │
│  │  Работа над ошибками (N)    │   │
│  └──────────────────────────────┘   │
└─────────────────────────────────────┘
```

**Характеристики:**
- Максимальная ширина: `max-w-4xl`
- Сетка: `grid-cols-2 gap-8`
- Карточки с эффектом тетради
- Условный блок "Работа над ошибками"

### 3. Страница учебных материалов (Learn)

**Структура:**
```
┌─────────────────────────────────────┐
│  [←]  Предмет - Учебные материалы  │
│                                     │
│  ┌──────────┐ ┌──────────────────┐ │
│  │          │ │                  │ │
│  │ Содерж.  │ │   Контент        │ │
│  │ (sticky) │ │   (prose)        │ │
│  │          │ │                  │ │
│  │ - Раздел │ │   Заголовки      │ │
│  │   - Тема │ │   Параграфы      │ │
│  │   - Тема │ │   Блоки          │ │
│  │          │ │                  │ │
│  └──────────┘ └──────────────────┘ │
└─────────────────────────────────────┘
```

**Характеристики:**
- Максимальная ширина: `max-w-7xl`
- Сайдбар: `w-1/4` (sticky)
- Контент: `flex-1` с `prose prose-stone prose-lg`
- Автоматическое отслеживание активного раздела

### 4. Страница тренировки (Train)

**Структура:**
```
┌─────────────────────────────────────┐
│  [×]  Вопрос N из M    Предмет     │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                     │
│  ┌──────────────────────────────┐ │
│  │  Вопрос?                     │ │
│  │                              │ │
│  │  ○ Вариант 1                 │ │
│  │  ○ Вариант 2                 │ │
│  │  ○ Вариант 3                 │ │
│  │  ○ Вариант 4                 │ │
│  │                              │ │
│  │  [Проверить]                 │ │
│  └──────────────────────────────┘ │
└─────────────────────────────────────┘
```

**Характеристики:**
- Центрированный контейнер: `max-w-2xl`
- Прогресс-бар в хедере
- Карточка вопроса с вариантами ответов
- Модальные окна для результатов

---

## 🎨 Паттерны дизайна

### 1. Hand-Drawn Border (Ручная граница)

**Эффект:** Границы и тени выглядят нарисованными от руки.

```tsx
// Tailwind класс
className="border-2 border-ink shadow-hand-drawn"

// Или inline стиль
style={{
  boxShadow: '4px 4px 0px rgba(107, 102, 68, 0.2)',
  borderRadius: '8px 10px 9px 8px', // Неидеальное скругление
}}
```

**Использование:**
- Карточки
- Кнопки
- Контейнеры

### 2. Notebook Lines (Линии тетради)

**Эффект:** Декоративные горизонтальные линии, имитирующие тетрадь.

```tsx
<div className="relative">
  <div className="absolute top-0 left-0 w-full h-1 bg-ink opacity-10" />
  <div className="absolute top-8 left-0 w-full h-px bg-ink opacity-5" />
  <div className="absolute top-16 left-0 w-full h-px bg-ink opacity-5" />
  {/* ... */}
</div>
```

**Использование:**
- Карточки "Учебные материалы" и "Тренировка"
- Создает ощущение школьной тетради

### 3. Slight Rotation (Легкий поворот)

**Эффект:** Элементы слегка повернуты для органичности.

```tsx
<motion.div
  animate={{ rotate: -1 }} // Легкий поворот
  whileHover={{ rotate: 0 }} // Выравнивание при hover
>
```

**Использование:**
- Карточки на странице предмета
- Создает ощущение неидеальности, как в скетчбуке

### 4. Staggered Animation (Каскадная анимация)

**Эффект:** Элементы появляются последовательно.

```tsx
<motion.div
  initial={{ opacity: 0, y: 30 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ delay: 0.2, staggerChildren: 0.1 }}
>
  {/* Дочерние элементы */}
</motion.div>
```

**Использование:**
- Сетки карточек
- Списки элементов

### 5. Spring Animation (Пружинная анимация)

**Эффект:** Естественное, упругое движение.

```tsx
whileHover={{
  scale: 1.05,
  transition: {
    type: 'spring',
    stiffness: 400,
    damping: 10,
  },
}}
```

**Использование:**
- Hover-эффекты на интерактивных элементах
- Папки предметов

---

## 📱 Адаптивность

### Desktop-Only подход

Проект **оптимизирован только для Desktop** (≥1024px). На мобильных устройствах показывается сообщение.

#### Компонент DesktopOnly

```tsx
'use client'

import { useEffect, useState } from 'react'

export default function DesktopOnly({ children }: { children: React.ReactNode }) {
  const [isDesktop, setIsDesktop] = useState(false)

  useEffect(() => {
    const checkDesktop = () => {
      setIsDesktop(window.innerWidth >= 1024)
    }

    checkDesktop()
    window.addEventListener('resize', checkDesktop)

    return () => window.removeEventListener('resize', checkDesktop)
  }, [])

  if (!isDesktop) {
    return (
      <div className="flex items-center justify-center h-screen bg-canvas">
        <p className="text-ink text-xl font-serif text-center px-8">
          Пожалуйста, откройте сайт с компьютера для учебы.
        </p>
      </div>
    )
  }

  return <>{children}</>
}
```

### Минимальные размеры

- **Минимальная ширина**: 1024px
- **Рекомендуемая ширина**: 1280px+
- **Оптимальная ширина**: 1920px

### Адаптивные элементы

Некоторые элементы используют адаптивные классы:

```tsx
// Адаптивный размер заголовка
<h1 className="text-3xl md:text-4xl font-bold text-ink">

// Адаптивная сетка (если понадобится)
<div className="grid grid-cols-1 md:grid-cols-2 gap-8">
```

---

## 📖 Руководство по использованию

### Создание новой карточки

```tsx
<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  whileHover={{ scale: 1.05 }}
  whileTap={{ scale: 0.98 }}
  className="bg-[#FFFCF5] border-2 border-ink p-8 rounded-lg cursor-pointer relative overflow-hidden"
  style={{
    boxShadow: '4px 4px 0px rgba(107, 102, 68, 0.2)',
    borderRadius: '8px 10px 9px 8px',
  }}
>
  {/* Декоративные линии (опционально) */}
  <div className="absolute top-0 left-0 w-full h-1 bg-ink opacity-10" />
  
  {/* Контент */}
  <div className="relative z-10">
    <h2 className="text-2xl font-serif font-bold text-ink mb-4">
      Заголовок
    </h2>
    <p className="text-ink opacity-70 text-lg">
      Описание
    </p>
  </div>
</motion.div>
```

### Создание нового заголовка

```tsx
// H1 (главный заголовок страницы)
<h1 className="text-4xl font-serif font-bold text-ink mb-8">
  Название страницы
</h1>

// H2 (заголовок раздела)
<h2 className="text-2xl font-semibold text-ink mb-4">
  Раздел
</h2>

// H3 (подзаголовок)
<h3 className="text-xl font-bold text-ink mb-3">
  Подраздел
</h3>
```

### Создание информационного блока

```tsx
// Синий блок (информация)
<div className="bg-blue-50 border-l-4 border-blue-500 p-4 rounded-r-lg mb-4">
  <p className="mb-0">
    Информация
  </p>
</div>

// Желтый блок (примечание)
<div className="bg-yellow-50 border-l-4 border-yellow-400 p-4 my-6 italic">
  <p className="mb-0">
    Примечание
  </p>
</div>

// Серый блок (контекст)
<div className="bg-gray-50 p-6 rounded-lg border border-gray-200">
  <h3 className="text-xl font-bold text-ink mb-3">
    Заголовок
  </h3>
  <p className="mb-0">
    Контент
  </p>
</div>
```

### Создание кнопки

```tsx
<motion.button
  whileHover={{ scale: 1.1 }}
  whileTap={{ scale: 0.95 }}
  onClick={handleClick}
  className="p-3 rounded-lg bg-white border-2 border-ink shadow-hand-drawn hover:bg-accent-gold transition-colors"
  aria-label="Описание действия"
>
  <Icon className="w-6 h-6 text-ink" />
</motion.button>
```

### Добавление анимации

```tsx
// Простое появление
<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.4 }}
>
  {/* Контент */}
</motion.div>

// С задержкой
<motion.div
  initial={{ opacity: 0, x: -20 }}
  animate={{ opacity: 1, x: 0 }}
  transition={{ delay: 0.2 }}
>
  {/* Контент */}
</motion.div>
```

---

## 🎯 Чеклист дизайна

При создании новых компонентов проверьте:

- [ ] Используется правильная цветовая палитра (`bg-canvas`, `text-ink`, `accent-gold`)
- [ ] Применена правильная типографика (Merriweather для заголовков, Nunito для текста)
- [ ] Добавлен hand-drawn эффект (тень, неидеальное скругление)
- [ ] Реализованы анимации (появление, hover, tap)
- [ ] Компонент обернут в `DesktopOnly`
- [ ] Используются семантические HTML-теги
- [ ] Добавлены `aria-label` для доступности
- [ ] Соблюдена иерархия заголовков
- [ ] Правильные отступы и размеры (`p-8`, `gap-8`, и т.д.)
- [ ] Контент читаем и структурирован

---

## 📚 Дополнительные ресурсы

### Файлы конфигурации

- `tailwind.config.ts` — конфигурация Tailwind CSS и кастомные цвета
- `src/app/globals.css` — глобальные стили и утилиты
- `src/app/components/` — компоненты UI

### Полезные ссылки

- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- [Framer Motion Documentation](https://www.framer.com/motion/)
- [Lucide Icons](https://lucide.dev/)
- [Google Fonts](https://fonts.google.com/)

---

## 🔄 Версионирование дизайна

**Версия:** 1.0.0  
**Последнее обновление:** 2024

### История изменений

- **v1.0.0** — Первоначальная версия дизайн-системы
  - Базовая цветовая палитра
  - Типографика Merriweather + Nunito
  - Hand-drawn эффекты
  - Анимации Framer Motion
  - Desktop-only подход

---

**Создано для проекта Bagrut Website**  
*Образовательная платформа для подготовки к экзаменам Багрут*

