# Анализ архитектуры проекта Bagrut Website

## 📊 Анализ текущей структуры

### Тип проекта
- **Фреймворк**: Next.js 14 (App Router)
- **Стек**: React 18, TypeScript, Tailwind CSS, Framer Motion
- **Тип приложения**: SPA с SSR возможностями (образовательная платформа)
- **Масштаб**: Малый-средний проект

### Текущая структура
```
Bagrut/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── components/         # ❌ ПРОБЛЕМА: компоненты внутри app/
│   │   │   ├── features/
│   │   │   └── ui/
│   │   ├── hooks/              # ❌ ПРОБЛЕМА: хуки внутри app/
│   │   ├── globals.css
│   │   ├── layout.tsx
│   │   ├── page.tsx
│   │   └── subject/[id]/
│   ├── data/                   # ✅ Хорошо: данные отдельно
│   │   ├── shared/
│   │   └── subjects/
│   ├── lib/                    # ⚠️ Пустая папка
│   └── types/                  # ⚠️ Пустая папка
```

---

## 🔴 Выявленные проблемы

### 1. **Нарушение структуры Next.js App Router**
- **Проблема**: Компоненты и хуки находятся внутри `app/`, что не является best practice
- **Почему плохо**: 
  - `app/` предназначен только для роутинга и layout'ов
  - Усложняет навигацию по коду
  - Смешивает роутинг с бизнес-логикой

### 2. **Длинные относительные импорты**
```typescript
// Текущий подход (плохо)
import DesktopOnly from '../../../components/ui/DesktopOnly'
import { useProgress } from '../../../hooks/useProgress'
```
- **Проблема**: Хрупкие пути, сложно рефакторить
- **Решение**: Использовать path aliases (`@/shared/ui`, `@/features/...`)

### 3. **Отсутствие разделения на слои**
- Нет четкого разделения на:
  - `shared/` - переиспользуемые компоненты/утилиты
  - `features/` - бизнес-фичи
  - `entities/` - бизнес-сущности (Subject, Question, Progress)
  - `widgets/` - сложные композиции компонентов

### 4. **Типы определены в хуках**
```typescript
// useProgress.ts - типы должны быть в отдельном файле
export type SubjectProgress = { ... }
export type UserProgress = { ... }
```
- **Проблема**: Типы смешаны с логикой
- **Решение**: Вынести в `src/shared/types/` или `src/entities/progress/types.ts`

### 5. **Плохое именование компонентов**
- `CivicsContent1.tsx`, `CivicsContent2.tsx` - неинформативные имена
- **Решение**: Использовать семантические имена (`Chapter1.tsx`, `Chapter2.tsx`)

### 6. **Отсутствие barrel exports**
- Нет `index.ts` файлов для упрощения импортов
- **Решение**: Добавить barrel exports в каждой папке

### 7. **Пустые папки**
- `lib/` и `types/` пустые, но присутствуют в структуре
- **Решение**: Либо использовать, либо удалить

### 8. **Смешение ответственностей**
- Компоненты материалов (`CivicsContent`) находятся в `features/subjects/materials`
- Это больше похоже на контент, чем на фичу
- **Решение**: Вынести в `entities/subject/content/` или `shared/content/`

### 9. **Нет констант для магических значений**
```typescript
// В page.tsx
const subjects: Subject[] = [
  { id: 'history', label: 'История', ... },
  // ...
]
```
- **Проблема**: Данные захардкожены в компоненте
- **Решение**: Вынести в `src/shared/config/subjects.ts`

### 10. **Неиспользуемые path aliases**
- В `tsconfig.json` есть `@/*`, но импорты используют относительные пути
- **Решение**: Настроить и использовать path aliases

---

## ✅ Предлагаемая архитектура

### Принцип: Feature-Sliced Design (адаптированный)

```
src/
├── app/                          # Next.js App Router (только роутинг)
│   ├── layout.tsx
│   ├── page.tsx
│   ├── globals.css
│   └── subject/[id]/
│       ├── page.tsx
│       ├── learn/page.tsx
│       └── train/page.tsx
│
├── shared/                       # Переиспользуемый код
│   ├── ui/                       # Базовые UI компоненты
│   │   ├── desktop-only/
│   │   │   ├── DesktopOnly.tsx
│   │   │   └── index.ts
│   │   ├── toast/
│   │   │   ├── Toast.tsx
│   │   │   └── index.ts
│   │   └── index.ts              # Barrel export
│   │
│   ├── hooks/                    # Переиспользуемые хуки
│   │   ├── use-progress/
│   │   │   ├── useProgress.ts
│   │   │   ├── types.ts
│   │   │   └── index.ts
│   │   └── index.ts
│   │
│   ├── lib/                      # Утилиты и хелперы
│   │   ├── storage.ts            # localStorage helpers
│   │   └── index.ts
│   │
│   ├── config/                   # Конфигурация и константы
│   │   ├── subjects.ts           # Конфигурация предметов
│   │   └── index.ts
│   │
│   └── types/                    # Общие типы
│       └── index.ts
│
├── entities/                     # Бизнес-сущности
│   ├── subject/
│   │   ├── model/
│   │   │   ├── types.ts          # Subject, SubjectId, etc.
│   │   │   └── index.ts
│   │   ├── content/              # Контент по предметам
│   │   │   ├── civics/
│   │   │   │   ├── Chapter1.tsx
│   │   │   │   ├── Chapter2.tsx
│   │   │   │   ├── index.tsx
│   │   │   │   └── index.ts
│   │   │   ├── default/
│   │   │   │   └── DefaultContent.tsx
│   │   │   └── index.ts
│   │   └── index.ts
│   │
│   ├── question/
│   │   ├── model/
│   │   │   ├── types.ts          # Question interface
│   │   │   └── index.ts
│   │   └── index.ts
│   │
│   └── progress/
│       ├── model/
│       │   ├── types.ts          # SubjectProgress, UserProgress
│       │   └── index.ts
│       └── index.ts
│
├── features/                     # Бизнес-фичи
│   ├── backpack/
│   │   ├── ui/
│   │   │   ├── HandDrawnFolder.tsx
│   │   │   └── index.ts
│   │   └── index.ts
│   │
│   ├── subject-navigation/
│   │   ├── ui/
│   │   │   └── SubjectCard.tsx
│   │   └── index.ts
│   │
│   └── training/
│       ├── ui/
│       │   └── QuestionCard.tsx
│       └── index.ts
│
├── widgets/                      # Сложные композиции (опционально)
│   └── subject-page/
│       ├── ui/
│       │   └── SubjectPageLayout.tsx
│       └── index.ts
│
└── data/                         # Статические данные
    ├── subjects/
    │   ├── ezrahut/
    │   │   ├── questions/
    │   │   └── index.ts
    │   ├── history/
    │   ├── literature/
    │   └── tanakh/
    └── shared/
        └── mockQuestions.ts
```

---

## 🔧 Ключевые изменения

### 1. **Вынос компонентов из app/**
- **Было**: `app/components/features/...`
- **Стало**: `src/features/...` и `src/shared/ui/...`
- **Почему**: Разделение ответственности, соответствие best practices Next.js

### 2. **Организация по слоям (FSD)**
- **shared/** - переиспользуемый код (UI, хуки, утилиты)
- **entities/** - бизнес-сущности (Subject, Question, Progress)
- **features/** - бизнес-фичи (backpack, training)
- **widgets/** - сложные композиции (опционально)

### 3. **Path aliases**
```typescript
// tsconfig.json
{
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"],
      "@/shared/*": ["./src/shared/*"],
      "@/entities/*": ["./src/entities/*"],
      "@/features/*": ["./src/features/*"],
      "@/widgets/*": ["./src/widgets/*"],
      "@/data/*": ["./src/data/*"]
    }
  }
}
```

### 4. **Barrel exports**
Каждая папка имеет `index.ts`:
```typescript
// shared/ui/index.ts
export { DesktopOnly } from './desktop-only'
export { Toast } from './toast'
```

### 5. **Разделение типов**
- Типы рядом с логикой (colocation)
- Общие типы в `shared/types/`
- Типы сущностей в `entities/{entity}/model/types.ts`

### 6. **Конфигурация вынесена**
```typescript
// shared/config/subjects.ts
export const SUBJECTS_CONFIG = [
  { id: 'history', label: 'История', ... },
  // ...
]
```

---

## 📋 План миграции

### Этап 1: Подготовка (безопасно, не ломает код)
1. ✅ Обновить `tsconfig.json` с новыми path aliases
2. ✅ Создать новую структуру папок
3. ✅ Создать barrel exports (`index.ts`) в новых папках

### Этап 2: Миграция shared слоя
1. Переместить `app/components/ui/` → `shared/ui/`
2. Переместить `app/hooks/` → `shared/hooks/`
3. Вынести типы из хуков в отдельные файлы
4. Обновить импорты в существующих файлах

### Этап 3: Миграция entities
1. Создать `entities/subject/model/types.ts`
2. Создать `entities/question/model/types.ts`
3. Создать `entities/progress/model/types.ts` (вынести из useProgress)
4. Переместить контент: `app/components/features/subjects/materials/` → `entities/subject/content/`

### Этап 4: Миграция features
1. Переместить `app/components/features/backpack/` → `features/backpack/`
2. Создать конфигурацию предметов в `shared/config/subjects.ts`
3. Обновить импорты в `app/page.tsx`

### Этап 5: Обновление импортов в app/
1. Обновить все импорты в `app/**/*.tsx` на использование path aliases
2. Удалить старые папки `app/components/` и `app/hooks/`

### Этап 6: Финальная очистка
1. Удалить пустые папки
2. Проверить все импорты
3. Запустить линтер и исправить ошибки

---

## 🎯 Дополнительные рекомендации

### 1. **Именование файлов**
- Компоненты: `PascalCase.tsx` ✅ (уже используется)
- Хуки: `use-kebab-case.ts` или `useCamelCase.ts`
- Утилиты: `kebab-case.ts`
- Типы: `types.ts` или `model.ts`

### 2. **Colocation**
Хранить связанные файлы рядом:
```
shared/ui/toast/
├── Toast.tsx
├── Toast.test.tsx      # Тесты рядом
├── toast.module.css    # Стили рядом (если нужны)
└── index.ts
```

### 3. **ESLint правила**
Добавить правила для path aliases:
```json
{
  "rules": {
    "no-restricted-imports": [
      "error",
      {
        "patterns": ["../../../*"]
      }
    ]
  }
}
```

### 4. **Документация**
Создать `ARCHITECTURE.md` с описанием структуры для команды

### 5. **Масштабируемость**
- При добавлении нового предмета: добавить в `data/subjects/` и `entities/subject/content/`
- При добавлении новой фичи: создать папку в `features/`
- При добавлении нового UI компонента: добавить в `shared/ui/`

---

## ❓ Вопросы для уточнения

1. **Размер команды**: Работаете один или в команде?
2. **Legacy код**: Есть ли код, который нельзя трогать?
3. **Тестирование**: Планируете ли добавлять тесты?
4. **Интернационализация**: Нужна ли поддержка нескольких языков?
5. **API**: Планируется ли интеграция с backend API?

---

## 🚀 Быстрый старт миграции

Если хотите начать миграцию, я могу:
1. Создать новую структуру папок
2. Настроить path aliases
3. Постепенно переместить файлы
4. Обновить все импорты

Готов начать по вашему запросу! 🎯

