# 🚀 Инструкция по деплою на GitHub Pages

## 📋 Что было сделано

1. ✅ Создан GitHub Actions workflow (`.github/workflows/deploy.yml`)
2. ✅ Настроен `next.config.js` с `basePath: '/Bagrut_website'`
3. ✅ Проект уже настроен для статического экспорта (`output: 'export'`)

## 🔧 Шаги для деплоя

### 1. Включите GitHub Pages в настройках репозитория

1. Перейдите в ваш репозиторий на GitHub: `https://github.com/for21ever19/Bagrut_website`
2. Откройте **Settings** → **Pages**
3. В разделе **Source** выберите:
   - **Source**: `GitHub Actions`
4. Сохраните изменения

### 2. Запушьте изменения в репозиторий

```bash
# Добавьте все изменения
git add .

# Создайте коммит
git commit -m "Настройка деплоя на GitHub Pages"

# Запушьте в main ветку
git push origin main
```

### 3. Проверьте деплой

1. После пуша GitHub Actions автоматически запустит workflow
2. Перейдите в раздел **Actions** вашего репозитория
3. Дождитесь завершения workflow (обычно 2-3 минуты)
4. После успешного деплоя ваш сайт будет доступен по адресу:
   ```
   https://for21ever19.github.io/Bagrut_website/
   ```

## 🧪 Локальное тестирование

Перед деплоем можно протестировать сборку локально:

```bash
cd Bagrut
npm run build
```

Собранные файлы будут в папке `Bagrut/out/`. Вы можете протестировать их локально:

```bash
# Установите простой HTTP сервер (если еще не установлен)
npm install -g serve

# Запустите сервер из папки out
cd out
serve -p 3000
```

Откройте `http://localhost:3000/Bagrut_website/` в браузере.

## ⚙️ Конфигурация

### basePath

В `next.config.js` установлен `basePath: '/Bagrut_website'`, потому что:
- Репозиторий называется `Bagrut_website`
- GitHub Pages размещает сайт по адресу `username.github.io/Bagrut_website/`

Если вы хотите изменить это:
1. Измените `basePath` в `next.config.js`
2. Обновите workflow файл (если нужно)

### Автоматический деплой

Workflow автоматически запускается при:
- Пуше в ветку `main`
- Ручном запуске через **Actions** → **Deploy to GitHub Pages** → **Run workflow**

## 🔍 Устранение проблем

### Сайт не обновляется

1. Проверьте, что workflow успешно завершился в разделе **Actions**
2. Подождите 1-2 минуты (GitHub Pages может кешировать)
3. Очистите кеш браузера (Ctrl+Shift+R или Cmd+Shift+R)

### Ошибки при сборке

1. Проверьте логи в разделе **Actions**
2. Убедитесь, что все зависимости установлены (`npm ci`)
3. Проверьте, что Node.js версия совместима (используется версия 18)

### Неправильные пути к ресурсам

Если изображения или другие ресурсы не загружаются:
1. Убедитесь, что `basePath` и `assetPrefix` установлены правильно
2. Проверьте, что пути начинаются с `/Bagrut_website/`
3. Используйте относительные пути или Next.js компонент `Image`

## 📝 Дополнительные настройки

### Изменение домена

Если вы хотите использовать кастомный домен:
1. Добавьте файл `CNAME` в папку `public/`
2. В файле укажите ваш домен (например: `example.com`)
3. Настройте DNS записи согласно инструкциям GitHub

### Отключение автоматического деплоя

Если нужно отключить автоматический деплой:
1. Откройте `.github/workflows/deploy.yml`
2. Удалите или закомментируйте секцию `on.push`

## ✅ Чеклист перед деплоем

- [ ] Все изменения закоммичены
- [ ] Проект собирается локально без ошибок (`npm run build`)
- [ ] GitHub Pages включен в настройках репозитория
- [ ] Workflow файл находится в `.github/workflows/deploy.yml`
- [ ] `basePath` настроен правильно в `next.config.js`

## 🎉 Готово!

После выполнения всех шагов ваш сайт будет автоматически деплоиться при каждом пуше в `main` ветку.

**URL вашего сайта:** `https://for21ever19.github.io/Bagrut_website/`

